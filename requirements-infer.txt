# requirements-infer.txt
# Inference-side dependencies (install on local RTX 3050, 4 GB VRAM).
# Much lighter than training deps — no optimizer, no trainer, no datasets.
# On local machine: pip install -r requirements-infer.txt

# Core
torch>=2.2.0,<3.0.0
numpy>=1.24.0,<2.0.0

# 4-bit quantized inference
bitsandbytes>=0.43.0
transformers>=4.40.0,<5.0.0    # Needed for qlora_finetune checkpoint loading
peft>=0.10.0                   # Needed for LoRA adapter loading (Mode B)

# Tokenizer
tokenizers>=0.19.0

# Config
PyYAML>=6.0

# Local API server (optional — comment out if you only need CLI generation)
fastapi>=0.110.0
uvicorn>=0.29.0

# Drive sync (optional — install rclone separately via OS package manager)
# gdown>=5.1.0   # Alternative: download from Google Drive shareable links

# Optional: GGUF export path via llama-cpp-python
# Requires a C++ compiler and cmake. Install separately:
#   pip install llama-cpp-python --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cu121
# llama-cpp-python>=0.2.0

# Utilities
tqdm>=4.66.0
